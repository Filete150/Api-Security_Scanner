openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0

paths:
  /users/{id}:
    get:
      responses:
        "200":
          description: OK

  /login:
    post:
      responses:
        "200":
          description: OK

  /products:
    get:
      responses:
        "200":
          description: OK
